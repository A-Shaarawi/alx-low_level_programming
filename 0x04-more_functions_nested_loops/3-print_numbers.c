#include "main.h"
#include <stdio.h>

/**
 * print_numbers - returns the largest of 3 numbers
 * Return: largest number
 */

void print_numbers(void)
{
for (int i = 48; i < 58; i++)
{
putchar(i);
}
putchar('\n');
}
