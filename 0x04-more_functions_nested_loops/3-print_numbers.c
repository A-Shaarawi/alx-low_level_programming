#include "main.h"
#include <stdio.h>

/**
 * print_numbers - returns the largest of 3 numbers
 * Return: largest number
 */

void print_numbers(void)
{
int a = 48;
while (a < 58)
{
putchar(a);
a++;
}
putchar('\n');
}
