#include "main.h"
#include <stdio.h>

/**
 * _isdigit - returns the largest of 3 numbers
 * @c: para
 * Return: largest number
 */

int _isdigit(int c)
{
if (c >= '0' && c <= '9')
{
return (1);
}
else
{
return (0);
}
}
