#include "main.h"
#include <stdio.h>

/**
 * _strlen - funtion
 * @s: parameter
 * Return: number
 */

int _strlen(char *s)
{
	int count = 0;

	while (*s != NULL)
	{
		cout++;
		*s++;
	}
	return (count);
}
