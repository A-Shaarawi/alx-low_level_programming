#include "main.h"
#include <stdio.h>

/**
 * *_strcpy - funtion
 * @dest: parameter
 * @src: parameter
 * Return: char
 */

char *_strcpy(char *dest, char *src)
{
	int i = 0;

	int j = 0;

	while (*(src + i) != '\0')
	{
		i++;
	}
	for ( ; x < l ; x++)
	{
		dest[x] = src[x];
	}
	dest[i] = 0;
	return (dest);
}
